version: "3.9"

services:
  frontend:
    build:
      context: .
      target: frontend
    ports:
      - "3000:3000"

  backend:
    build:
      context: .
      target: backend
    ports:
      - "8000:8000"
